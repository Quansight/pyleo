import setuptools


setuptools.setup(name='pyleo',
                 version='0.1.0',
                 packages=setuptools.find_packages(),
                 install_requires=[
                    'pandas>=1.4',
                 ])
